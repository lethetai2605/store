<%= form_with(model: item_review) do |form| %>
  <% if item_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_review.errors.count, "error") %> prohibited this item_review from being saved:</h2>

      <ul>
        <% item_review.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.number_field :item_id %>
  </div>

  <div class="field">
    <%= form.label :review %>
    <%= form.text_area :review %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
